{{> header }}

<main>
    <ul>
        {{#each posts }}
        <li>
            <div>
                <a href="/{{ path }}">{{ title }}</a>
            </div>
            <div class="date">{{ date }}</div>
        </li>
        {{/each}}
    </ul>
</main>

{{> footer }}
