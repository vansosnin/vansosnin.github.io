<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>{{ title }} | {{ sitename }}</title>
    </head>
    <body>
        <article>
            <h1>{{ title }}</h1>
            <div class="meta">{{#if date}}Опубликовано: {{ date }}{{/if}}</div>
            {{{ contents }}}
        </article>
    </body>
</html>
